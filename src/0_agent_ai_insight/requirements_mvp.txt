# AI Business Insights Generator - MVP Requirements
# Minimal dependencies for the simplified 4-phase workflow

# ============================================================================
# CORE DATA SCIENCE
# ============================================================================
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
# Provides: KMeans, RandomForestClassifier, StandardScaler

# ============================================================================
# LANGCHAIN & OPENAI (REQUIRED)
# ============================================================================
langchain-core>=0.1.0
# Provides: HumanMessage

langchain-openai>=0.1.0
# Provides: ChatOpenAI (works with OpenAI-compatible endpoints including Gemini)

openai>=1.0.0
# Provides: OpenAI SDK (supports custom base_url for Gemini endpoint)

# ============================================================================
# WEB INTERFACE
# ============================================================================
gradio>=4.0.0
# Provides: Chatbot interface with public link support

# ============================================================================
# UTILITIES
# ============================================================================
python-dotenv>=1.0.0
# Provides: load_dotenv for .env file support

requests>=2.31.0
# Provides: HTTP requests for HuggingFace data download fallback

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
# Install all dependencies:
#   pip install -r requirements_mvp.txt
#
# Or install individually:
#   pip install pandas numpy scikit-learn
#   pip install langchain-core langchain-openai openai
#   pip install gradio python-dotenv requests
#
# ============================================================================
# .ENV CONFIGURATION
# ============================================================================
# The MVP uses OpenAI-compatible interface for Gemini. Set these in .env:
#
#   OPENAI_BASE_URL="https://generativelanguage.googleapis.com/v1beta/openai/"
#   OPENAI_API_KEY="your-gemini-api-key-here"
#
# This allows using Gemini models (gemini-1.5-flash, gemini-1.5-pro) through
# the OpenAI SDK, which aligns with Vector Institute bootcamp patterns.
#
# ============================================================================
# OPTIONAL BUT RECOMMENDED
# ============================================================================
datasets>=2.0.0
# Provides: HuggingFace datasets loader (more robust than direct URL download)
# Recommended for production use, but not strictly required (URL fallback available)

# ============================================================================
# OPTIONAL (NOT REQUIRED FOR MVP)
# ============================================================================
# langgraph>=0.0.20          # Only needed for complex state graphs (not in MVP)
# scipy>=1.10.0              # Only needed for advanced statistics (not in MVP)
# pypdf>=3.0.0               # Only needed for PDF reading (not in MVP)
# e2b-code-interpreter       # Only needed for sandboxed execution (not in MVP)
# langfuse>=3.0.0            # Only needed for tracing/monitoring (optional)
